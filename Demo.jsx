import React from 'react'

const Demo = () => {
    let p = 51;
    const w = 60;
    for(i = 1; i => 10; i++; ) {

    }

    if(w > 10) {
        return (
            <p>w is greater than 10</p>
        )
    } else {
        <p>w is less than 10</p>
    }
  return (
    <section>
      <main className='p'>
        {w > 10 ? 'w is greater than 10' : 'w is less than 10'}
      </main>
    </section>
    
  )
}

export default Demo